defaults: &defaults
  app_name: kamyshko
  user: webapps
  group: developers
  app_root: '/var/webapps/kamyshko/current'
  sphinx_bin_path: '/usr/local/bin'

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
